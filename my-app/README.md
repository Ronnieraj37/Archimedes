# Archimedes Agent – Frontend

Next.js app for the Archimedes DeFi terminal (AI chat, Pools, Swap, Invest).

- **Project overview and architecture:** see repo root [README.md](../README.md).
- **Deploy and env setup:** see [DEPLOYMENT.md](../DEPLOYMENT.md).

## Run locally

```bash
yarn install
cp .env.example .env   # set ANTHROPIC_API_KEY, NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID
yarn dev
```

Open [http://localhost:3000](http://localhost:3000). Wallet is **Base Sepolia** only.
